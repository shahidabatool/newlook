import { registerRootComponent } from 'expo';

import App from './App';

// This is the universal entry point for Expo apps.
// It will delegate the rendering to the correct platform-specific logic.
registerRootComponent(App);
